services:
  backend:
    build: ./backend
    ports:
      - "3001:3000"
    restart: unless-stopped

  frontend:
    build:
      context: ./front
      args:
        VITE_API_URL: http://localhost:3001
    ports:
      - "8082:80"
    depends_on:
      - backend
    restart: unless-stopped
